# Bright前端开发任务清单

## 核心说明

🔥 已完成：标注具体实现步骤 + 最终效果，开源者可直接参考落地

📌 待完成：标注难度星级+ 核心目标 + 落地思路，明确后续迭代方向

难度定义：⭐ 基础（纯前端语法 / 组件封装，易落地）、⭐⭐ 中等（需业务适配 / 第三方库集成）、⭐⭐⭐ 进阶（需工程化 / 跨端 / 实时通信能力）

## 整体代办清单（优先级排序）

| 优先级 | 代办项              |  难度  | 核心价值     |
| :----: | :------------------ | :----: | :----------- |
|   高   | 页面滚动位置保留    |   ⭐   | 基础体验优化 |
|   高   | 表单自动保存        |   ⭐   | 防止数据丢失 |
|   中   | 实时评论(WebSocket) | ⭐⭐⭐ | 核心交互升级 |
|   低   | 自定义组件库发布    | ⭐⭐⭐ | 工程化复用   |

## 一、基础体验增强

### 🔥 已完成（9 项）

1. 页面加载骨架屏（Skeleton）
   实现步骤：
   复用 Element Plus 的<el-skeleton/>组件，为 Card 组件添加骨架屏结构；
   Home 页面初始化设置loading = true，onMounted 中通过 setTimeout 模拟后端数据加载；
   数据加载完成后设置loading = false，骨架屏切换为真实内容；
   实现效果：进入主页加载时显示骨架卡片，替代纯 loading，减少 “等待感”。
2. 路由切换过渡动画
   实现步骤：
   路由切换本质是<router-view>内部组件替换，使用<Transition>包裹 Layout 组件中 rightmain 的<router-view>；
   配置淡入淡出 CSS 样式，实现组件切换动效；
   实现效果：路由切换时页面 “淡入淡出”，替代原闪烁效果，交互更流畅。
3. 全局加载遮罩（Loading）
   实现步骤：
   Pinia 中创建useLoadingStore管理全局加载状态；
   封装 Axios 拦截器（@/config/request.js），请求发起时开启加载、响应完成后关闭；
   开发全局 Loading 组件，在 App.vue 中全局挂载；
   实现效果：异步请求时显示 “飞行员火速飞行” 全局遮罩，页面无乱跳，感知更友好。
4. 404/500 等错误页面定制
   实现步骤：
   路由配置中添加兜底规则path: '/:pathMatch(._)_'，匹配 404 页面；
   Axios 响应拦截器中捕获 500 服务器错误，给出友好警告提示；
   实现效果：无效路由跳转 404 页面，服务器错误触发统一提示。
5. 空状态组件
   实现步骤：
   基于el-empty封装通用 Empty 组件，设置组件占满容器并居中显示；
   实现效果：列表 / 表单无数据时显示友好提示（可参考 “个人作品” 模块效果）。
6. 回到顶部按钮
   实现步骤：
   定义 props 灵活接收滚动容器参数；
   监听滚动事件，判断滚动距离控制按钮显隐；
   外层包裹<Transition>实现按钮淡入淡出动画；
   实现效果：滚动超过阈值显示按钮，点击快速回到顶部，动画过渡自然。
7. 面包屑导航
   实现步骤：直接复用 Element Plus 的<el-breadcrumb>组件，基于路由元信息渲染；
   实现效果：多级路由页面显示面包屑，支持快速返回上级页面。
8. 页面标题动态修改
   实现步骤：
   路由 meta 中配置title属性；
   全局后置路由守卫中读取 meta.title，通过document.title更新；
   文章详情页等动态页面，获取数据后手动更新document.title；
   实现效果：不同页面自动显示对应标题，文章页显示文章名称。
9. 暗黑模式切换
   实现步骤：
   base.scss中定义亮 / 暗两套 CSS 变量；
   Pinia 中创建主题状态管理（切换 / 持久化）；
   封装 ThemeToggle 组件，绑定切换逻辑；
   实现效果：一键切换全局主题，CSS 变量渐变过渡，无瞬间切换的生硬感。
10. 语言国际化（i18n）
    实现步骤：
    安装vue-i18n依赖，编写多语言文案文件；
    配置src/i18n.js并全局注册；
    业务中通过$t()/useI18n引用多语言文案；
    实现效果：支持文案 / 提示语多语言切换。

### 📌 待完成（3 项）

11. 页面刷新保留滚动位置
    难度：⭐
    核心目标：解决与 “回到顶部” 功能的冲突，刷新后恢复用户滚动位置
    落地思路：
    全局注册beforeunload事件；
    退出页面时记录滚动容器（#layout-main）的scrollTop；
    页面加载时读取缓存的scrollTop并恢复。
12. 页面缩放控制
    难度：⭐
    核心目标：支持快捷键 / 按钮调整页面缩放比例，适配不同屏幕需求
    落地思路：
    监听快捷键（Ctrl+/Ctrl-）或按钮点击事件；
    调整根节点font-size或transform: scale()实现缩放；
    限制缩放范围（如 0.8-1.2 倍），避免过度缩放。

# 二、表单增强功能（共 15 项）

### 🔥 已完成（4 项）

1. 表单一键重置
   实现步骤：
   基于 Element Plus 的el-form设置初始值；
   点击重置按钮调用resetFields方法，恢复表单默认状态；
   实现效果：一键清空表单输入，保留预设默认值。
2. 富文本编辑器
   实现步骤：
   封装 MdEditor 组件，集成 Vditor 富文本编辑器；
   实现效果：支持 Markdown 编辑 / 预览，后续可扩展 Vditor 更多功能。
3. 表单提交防抖
   实现步骤：
   封装 debounce 组合式函数；
   表单提交按钮绑定防抖后的提交方法；
   实现效果：防止短时间内重复点击提交，避免重复请求。
4. 确认框（Confirm）封装
   实现步骤：复用 Element Plus 的ElMessageBox + ElMessage组合，统一交互风格；
   实现效果：退出登录等操作触发确认弹窗，提示用户确认后执行。

### 📌 待完成（11 项）

5. 表单自动保存
   难度：⭐
   核心目标：定时 / 失焦时缓存表单数据到 localStorage，防止数据丢失
   落地思路：
   监听表单字段blur事件，或设置 30s 定时任务；
   将表单数据序列化后存入 localStorage；
   表单初始化时读取缓存数据回填。
6. 表单验证自定义规则
   难度：⭐
   核心目标：扩展手机号 / 身份证 / 邮箱等自定义校验规则
   落地思路：
   基于 Element Plus 的校验规则，扩展通用校验函数；
   全局注册校验规则，表单中直接引用。
7. 表单联动
   难度：⭐⭐
   核心目标：监听字段变化自动更新关联字段（如省市区联动）
   落地思路：
   使用watch监听目标字段；
   字段变化时请求 / 筛选关联数据，更新联动字段值。
8. 图片上传（单 / 多图）
   难度：⭐⭐
   核心目标：支持预览 / 裁剪 / 压缩 / 拖拽上传
   落地思路：
   基于el-upload封装上传组件；
   集成图片压缩 / 裁剪库，上传前处理图片；
   封装 Axios 上传逻辑，显示上传进度。
9. 文件上传
   难度：⭐⭐
   核心目标：支持断点续传 / 进度条 / 文件校验
   落地思路：
   基于 Blob 切片实现断点续传；
   校验文件类型 / 大小，超出限制给出提示；
   显示上传进度条，支持暂停 / 继续。
10. 下拉选择远程搜索
    难度：⭐
    核心目标：结合el-select的remote-method实现模糊搜索
    落地思路：
    配置el-select远程搜索属性；
    输入关键词时请求后端接口，返回匹配选项。
11. 级联选择器
    难度：⭐⭐
    核心目标：封装通用级联组件，支持静态 / 动态加载
    落地思路：
    基于el-cascader封装；
    动态加载模式下，点击父级选项请求子级数据。
12. 表单只读 / 编辑模式切换
    难度：⭐
    核心目标：一键切换表单状态，无需重复开发两套布局
    落地思路：
    定义readonly状态变量；
    表单元素绑定disabled或通过v-if切换显示模式。
13. 表单数据导出（Excel）
    难度：⭐⭐
    核心目标：将表单 / 列表数据导出为 Excel
    落地思路：集成xlsx库，将数据转换为 Excel 格式并下载。
14. 表单数据导入（Excel）
    难度：⭐⭐
    核心目标：解析 Excel 文件自动填充表单 / 列表
    落地思路：
    读取上传的 Excel 文件，解析为 JSON 数据；
    校验数据格式后回填到表单 / 列表。
15. 动态增减表单项
    难度：⭐⭐
    核心目标：支持表单项的新增 / 删除 / 排序
    落地思路：
    基于v-for渲染表单项；
    绑定新增 / 删除按钮事件，操作表单数组；
    集成拖拽库实现表单项排序。

# 三、数据展示与可视化（共 10 项）

### 📌 待完成（10 项）

1. 表格列宽拖拽
   难度：⭐⭐
   核心目标：支持表格列宽自定义，提升数据浏览体验
   落地思路：集成vue-draggable-resizable，为表格列添加拖拽调整宽度功能。
2. 表格列显隐切换
   难度：⭐⭐
   核心目标：保存用户列配置，实现个性化列展示
   落地思路：
   提供列显隐切换面板；
   将用户配置保存到 localStorage/Pinia，页面加载时恢复。
3. 表格行拖拽排序
   难度：⭐⭐
   核心目标：支持表格行拖拽调整顺序
   落地思路：集成vuedraggable，包裹表格行实现拖拽排序。
4. 表格分页 / 筛选 / 排序
   难度：⭐⭐
   核心目标：封装通用表格组件，统一处理分页 / 筛选逻辑
   落地思路：
   封装 Table 组件，内置分页 / 筛选 / 排序参数；
   统一请求参数格式，适配后端接口。
5. 表格数据导出
   难度：⭐⭐
   核心目标：支持导出当前页 / 全部数据为 Excel/CSV
   落地思路：基于xlsx库，区分 “当前页”“全部” 数据范围，导出对应内容。
6. 树形表格
   难度：⭐⭐
   核心目标：基于 Element Plus 实现树形结构数据展示
   落地思路：配置el-table的树形属性，适配树形数据格式。
7. 数据可视化图表
   难度：⭐⭐
   核心目标：封装折线 / 柱状 / 饼图等组件，展示博客数据
   落地思路：集成 ECharts5，封装通用图表组件，支持动态更新数据。
8. 数据大屏适配
   难度：⭐⭐⭐
   核心目标：实现大屏自适应，图表响应式调整
   落地思路：基于 vw/vh/rem 适配大屏尺寸，ECharts 监听窗口 resize 调整图表大小。
9. 标签页（Tab）复用
   难度：⭐⭐
   核心目标：缓存已打开 Tab 页，切换时不重复请求
   落地思路：
   基于keep-alive缓存 Tab 页组件；
   记录 Tab 页状态，切换时恢复数据。
10. 数据懒加载
    难度：⭐⭐
    核心目标：列表滚动到底部自动加载下一页
    落地思路：基于IntersectionObserver监听列表底部元素，触达时加载下一页数据。

# 四、交互与动画（共 10 项）

### 🔥 已完成（8 项）

1. 按钮点击防抖 / 节流
   实现步骤：
   封装防抖 / 节流工具函数；
   封装v-debounce/v-throttle自定义指令；
   main.js 中全局注册指令，按钮直接引用；
   实现效果：高频点击按钮仅触发一次操作，避免重复请求。
2. 弹窗拖拽
   实现步骤：
   绑定mousedown事件触发拖拽开始；
   mousemove事件中计算弹窗偏移量（节流优化）；
   mouseup事件停止拖拽，移除事件监听；
   实现效果：登录弹窗支持自由拖拽，操作流畅（可测试登录界面）。
3. 拖拽组件
   实现步骤：
   安装vue3-draggable；
   将<ul>替换为<Draggle tag="ul">；
   实现效果：首页卡片支持拖拽排序（可测试首页功能）。
4. 右键菜单
   实现步骤：
   监听目标元素contextmenu事件，阻止默认菜单；
   记录鼠标点击位置，显示自定义 Context 组件；
   点击外部 / 组件销毁时关闭菜单，移除事件监听；
   实现效果：点击左侧月亮 Icon 触发自定义右键菜单（可测试该功能）。
5. 提示框（Message）封装
   实现步骤：直接复用 Element Plus 的ElMessage，后续可基于此扩展自定义样式；
   实现效果：全局统一提示风格，支持不同类型（成功 / 错误 / 警告）提示。
6. 引导页 / 新手教程
   实现步骤：
   异步导入driver.js及样式（按需加载）；
   封装引导页组件，配置步骤（高亮 / 遮罩 / 提示）；
   注册页面首次访问时触发引导；
   实现效果：用户注册后触发分步引导，后续可按需扩展（仅注册页已实现）。
7. 数字滚动动画
   实现步骤：
   安装@tombcato/smart-ticker；
   导入组件及样式，绑定数值实现滚动动画；
   实现效果：右上角文本切换时触发数字 / 文本差分滚动动画（后续可扩展到倒计时）。
8. 滚动动画
   实现步骤：
   封装v-intersection-animate全局指令（基于IntersectionObserver）；
   全局注册指令并配置动画样式；
   组件中通过v-intersection-animate:slide-left引用；
   实现效果：左侧栏元素进入视口时触发滑入 / 渐入动画（可测试 leftbar）。

### 📌 待完成（2 项）

9. 弹窗自适应
   难度：⭐
   核心目标：弹窗大小随内容自适应，超出视口时滚动
   落地思路：
   监听弹窗内容变化，动态调整弹窗宽高；
   内容超出视口高度时，添加滚动条。
   补充：场景化动效（待细化落地）
   难度：⭐⭐
   核心目标：文章 / 评论 / 列表场景添加专属动效
   落地思路：
   文章标题：视口触发 “左滑入 + 渐显”；
   文章段落：逐行渐显动画；
   评论提交：新评论 “下滑入 + 缩放”；
   评论点赞：心跳缩放 + 颜色渐变；
   分页切换：当前页按钮脉冲动画。

# 五、权限与安全（共 10 项）

### 🔥 已完成（8 项）

1. 路由权限控制
   实现步骤：
   拆分路由文件：constantRoutes.js（公共路由）、asyncRoutes.js（权限路由）；
   封装permission.js全局路由守卫，基于用户权限过滤路由；
   拆分后置守卫到单独文件，规范路由结构；
   实现效果：基于用户权限动态生成可访问路由，路由结构更清晰。
2. 按钮级权限控制
   实现步骤：
   Pinia 中存储用户权限列表；
   封装v-permission指令，控制按钮显隐 / 禁用；
   main.js 中全局注册指令；
   实现效果：组件库中已展示该功能，可按需接入业务。
3. 接口权限校验
   实现步骤：
   Axios 请求拦截器：从 Pinia 获取 Token，添加到请求头；
   Axios 响应拦截器：
   401：清空 Token / 用户信息，弹出登录弹窗；
   403：提示 “权限不足”；
   其他错误：统一弹窗提示；
   实现效果：接口请求自动带 Token，权限错误统一处理。
4. Token 过期自动刷新
   实现步骤：
   Pinia 中存储accessToken和refreshToken双令牌；
   预留 Token 刷新逻辑（待后端对接）；
   实现效果：前端已预留双 Token 存储，后端对接后可无感刷新。
5. 登录状态持久化
   实现步骤：
   Pinia 中开启persist持久化，存储用户信息 / Token；
   页面刷新时从 Pinia 恢复状态，Axios 拦截器自动带 Token；
   实现效果：刷新页面后用户登录状态不丢失。
6. 防 XSS 攻击
   实现步骤：
   安装xss库，封装xssFilter.js工具（普通文本 / HTML 过滤）；
   用户输入 / 后端返回内容均经过 XSS 过滤；
   实现效果：过滤恶意脚本，防止 XSS 攻击。
7. 接口请求加密
   实现步骤：
   封装 AES 加密 / 解密工具；
   预留敏感接口参数加密逻辑（如密码）；
   实现效果：敏感数据传输前加密，防止数据泄露。
8. 登录密码强度校验
   实现步骤：预留前后端密码强度校验逻辑，待对接；
   实现效果：后续可检测密码复杂度，提示安全等级。

### 📌 待完成（2 项）

9. 防 CSRF 攻击
   难度：⭐⭐
   核心目标：请求头携带 CSRF Token，后端校验防止跨站请求伪造
   落地思路：
   后端生成 CSRF Token 并返回；
   前端存储 Token，非 GET 请求时携带到请求头；
   后端校验请求头 Token 与会话 Token 一致性。
10. 操作日志记录
    难度：⭐⭐⭐
    核心目标：记录用户关键操作，保存到后端
    落地思路：
    封装日志上报函数；
    监听登录 / 修改数据等关键操作，上报日志到后端。

# 六、性能优化功能（共 10 项）

### 🔥 已完成（9 项）

1. 路由懒加载
   实现步骤：
   将静态导入改为动态导入：const Home = () => import('./views/Home.vue')；
   验证：F12 查看打包后 chunk，首屏包体积减小；
   实现效果：首屏加载速度提升，代码包按需加载。
2. 组件懒加载
   实现步骤：
   使用defineAsyncComponent封装异步组件（区分页面 / 普通组件）；
   配置加载中 / 错误组件、超时时间等；
   实现效果：非首屏组件按需加载，减少初始加载体积。
3. 图片懒加载
   实现步骤：
   封装v-lazy指令（基于IntersectionObserver）；
   图片进入视口时加载，支持占位图 / 错误图；
   实现效果：仅加载可视区域图片，减少请求数（ElImage 已适配，展示底层原理）。
4. 缓存频繁请求数据
   实现步骤：
   封装cache.ts工具，兼容 Pinia/localStorage；
   接口层集成缓存逻辑，设置过期时间；
   实现效果：高频请求数据缓存，减少网络请求，提升加载速度。
5. 按需引入 UI 组件
   实现步骤：集成unplugin-auto-import+unplugin-vue-components，自动按需引入 Element Plus 组件；
   实现效果：打包体积减小，无需手动导入组件。
6. 预加载关键资源
   实现步骤：在index.html中通过<link rel="preload">预加载首屏关键样式；
   实现效果：首屏样式优先加载，减少白屏时间。
7. 响应式数据优化
   实现步骤：Home 页面中使用shallowReactive替代reactive（仅监听第一层）；
   实现效果：减少响应式开销，提升渲染性能。
8. 无用事件解绑
   实现步骤：
   避免匿名函数绑定事件；
   onUnmounted 中解绑滚动 / 鼠标等全局事件；
   实现效果：无内存泄漏，页面销毁后事件正常解绑。
9. 打包体积分析
   实现步骤：
   集成rollup-plugin-visualizer；
   添加脚本build:analyze，打包后生成体积分析旭日图；
   实现效果：直观查看依赖体积，定位大依赖优化。

### 📌 待完成（1 项）

10. 减少首屏请求
    难度：⭐⭐⭐
    核心目标：合并小接口 / SSR/SSG 减少首屏请求
    落地思路：
    后端创建聚合接口，合并多个小请求；
    可选：接入 SSR/SSG，预渲染首屏内容。

# 七、工程化与开发效率（共 10 项）

### 🔥 已完成（10 项）

1. 接口请求封装
   实现步骤：
   Axios 创建实例，配置基础地址 / 超时 / 请求头；
   封装请求 / 响应拦截器，统一处理 Token / 错误；
   实现效果：接口请求逻辑统一，便于维护（@/config/request.js）。
2. 全局常量管理
   实现步骤：
   封装constants.js，统一管理接口地址 / 枚举值；
   业务中引用常量，避免硬编码；
   实现效果：修改配置只需改常量文件，无需全局替换。
3. 通用工具函数
   实现步骤：封装utils/useUtils.ts，包含：
   精确类型判断（typeOf）；
   深拷贝（解决循环引用）；
   日期格式化（兼容多格式输入）；
   防抖 / 节流函数；
   空值判断 / Ref 值处理；
   实现效果：通用逻辑复用，减少重复代码。
4. 代码规范校验
   实现步骤：
   集成 ESLint+Prettier，配置规则文件；
   VSCode 配置自动格式化，Husky 提交前校验；
   实现效果：代码风格统一，提交前自动修复格式问题。
5. 提交规范校验
   实现步骤：
   集成 Husky+commitlint，约束提交信息格式；
   提交信息需符合type(scope): description规范；
   实现效果：提交记录清晰，便于生成 CHANGELOG。
6. 环境变量区分
   实现步骤：
   创建.env.development/.env.production；
   通过import.meta.env访问环境变量；
   实现效果：开发 / 生产环境配置隔离，敏感数据不暴露。
7. 组件自动注册
   实现步骤：
   components/index.js中通过import.meta.glob批量注册全局组件；
   main.js 中引入注册函数，异步加载优化首屏；
   实现效果：新增全局组件无需手动注册，减少重复代码。
8. 错误边界
   实现步骤：封装 ErrorBoundary 组件，包裹易出错组件（如 Home 的 Card）；
   实现效果：子组件渲染错误时，显示友好提示，避免页面崩溃。
9. Mock 数据
   实现步骤：集成vite-plugin-mock，本地模拟接口数据；
   实现效果：无需依赖后端，本地开发可模拟接口返回。
10. 打包体积分析
    实现步骤：同 “性能优化” 第 10 项，略。

# 八、移动端适配（共 8 项）

### 📌 待完成（8 项）

1. 移动端适配方案
   难度：⭐⭐
   核心目标：基于postcss-px-to-viewport实现 px 转 vw/vh
   落地思路：
   安装postcss-px-to-viewport；
   配置设计稿宽度，自动将 px 转为 vw/vh。
2. 触摸事件封装
   难度：⭐⭐
   核心目标：封装v-touch指令，支持点击 / 滑动 / 长按
   落地思路：监听移动端触摸事件，封装指令支持不同触摸行为。
3. 下拉刷新 / 上拉加载
   难度：⭐⭐
   核心目标：封装移动端列表下拉刷新、上拉加载组件
   落地思路：监听滚动 / 触摸事件，实现下拉刷新动画、上拉加载逻辑。
4. 软键盘适配
   难度：⭐⭐
   核心目标：监听软键盘弹出 / 收起，调整页面布局
   落地思路：监听resize/focus事件，软键盘弹出时调整表单位置。
5. 图片自适应
   难度：⭐
   核心目标：封装图片组件，支持移动端自适应缩放
   落地思路：基于max-width: 100%+object-fit实现图片自适应。
6. 手势识别
   难度：⭐⭐⭐
   核心目标：集成 hammer.js，支持双击 / 捏合 / 旋转等手势
   落地思路：集成 hammer.js，封装手势识别指令 / 组件。
7. 移动端复制功能
   难度：⭐
   核心目标：封装copyToClipboard函数，适配移动端剪贴板
   落地思路：兼容移动端剪贴板 API，封装通用复制函数。
8. 横屏 / 竖屏提示
   难度：⭐
   核心目标：监听屏幕旋转，提示用户竖屏使用
   落地思路：监听orientationchange事件，横屏时显示提示遮罩。

# 九、错误处理与监控（共 5 项）

### 🔥 已完成（4 项）

1. 全局错误捕获
   实现步骤：
   app.config.errorHandler捕获 Vue 运行时错误；
   错误信息输出控制台 + ElMessage 提示用户 + 预留后端上报；
   实现效果：组件渲染错误不崩溃，给出友好提示。
2. 接口错误统一处理
   实现步骤：Axios 响应拦截器中统一处理 400/500 等错误，给出提示；
   实现效果：接口错误提示统一，用户感知友好。
3. 接口超时处理
   实现步骤：
   Axios 配置timeout超时时间；
   响应拦截器中判断超时错误，提示用户重试；
   实现效果：请求超时后给出重试提示，提升弱网体验。
4. 性能监控
   实现步骤：
   封装performanceMonitor.js工具，基于 Performance API；
   监听路由切换，统计首屏加载 / 路由切换耗时；
   实现效果：页面中展示性能数据，后续可上报后端。

### 📌 待完成（1 项）

5. 前端日志上报
   难度：⭐⭐
   核心目标：将错误 / 操作日志上报到后端
   落地思路：
   封装日志上报函数；
   集成到全局错误 / 接口错误 / 用户操作中，异步上报日志。

# 十、扩展与集成功能（共 2 项 + 待扩展）

### 🔥 已完成（2 项）

1. 二维码生成 / 解析
   实现步骤：
   基于qrcode库生成二维码（文本 / 链接转 base64）；
   预留扫码解析逻辑（jsqr+ 摄像头流）；
   实现效果：封装二维码组件，输入文本自动生成二维码。
2. 基于 TOTP 二次验证
   实现步骤：
   预留前后端对接逻辑（二维码生成 / 验证码校验）；
   后端标记用户 TOTP 状态，危险操作需二次验证；
   实现效果：后续可集成到登录 / 删文章等流程，提升安全性。

### 📌 待扩展（待完成）

微信登录 / 支付
难度：⭐⭐⭐
核心目标：集成微信授权登录、微信支付
落地思路：对接微信开放平台 API，封装登录 / 支付逻辑。
地图集成
难度：⭐⭐
核心目标：集成高德 / 百度地图，展示位置信息
落地思路：接入地图 SDK，封装地图组件，适配业务场景。
消息推送
难度：⭐⭐⭐
核心目标：集成极光推送 / WebSocket，实现消息推送
落地思路：对接推送平台，封装推送监听 / 展示组件。
